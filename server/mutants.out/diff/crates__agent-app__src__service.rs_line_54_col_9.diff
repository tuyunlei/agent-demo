--- crates/agent-app/src/service.rs
+++ replace AuthService::login -> Result<LoginResult, AuthServiceError> with Ok(Default::default())
@@ -46,28 +46,17 @@
         }
     }
 
     pub async fn login(
         &self,
         email: &str,
         password: &str,
     ) -> Result<LoginResult, AuthServiceError> {
-        let user = self
-            .auth_port
-            .authenticate(email, password)
-            .await
-            .map_err(map_auth_error)?;
-
-        let token_pair = self.create_token_pair(&user.user_id)?;
-
-        Ok(LoginResult {
-            user_id: user.user_id,
-            token_pair,
-        })
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     pub async fn register(
         &self,
         email: &str,
         password: &str,
         display_name: &str,
     ) -> Result<LoginResult, AuthServiceError> {
