[package]
name = "agent-proto"
version = "0.1.0"
edition.workspace = true

[dependencies]
agent-types = { path = "../agent-types" }
tonic = "0.12"
prost = "0.13"
prost-types = "0.13"

[build-dependencies]
tonic-build = "0.12"
