# crates/ — Workspace Crates

See `server/AGENTS.md` for global architecture, layer definitions, and dependency rules.

Each crate has its own `AGENTS.md` with crate-specific constraints. Read it before making changes.

Some crates have subdirectory-level `AGENTS.md` files for deeper guidance (e.g. `agent-channel/src/grpc/`, `agent-context/src/sections/`, `agent-tools/src/builtin/`, `agent-storage/src/pg/`).
