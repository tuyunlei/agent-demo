[package]
name = "agent-domain"
version = "0.1.0"
edition.workspace = true

[dependencies]
agent-types = { path = "../agent-types" }
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
